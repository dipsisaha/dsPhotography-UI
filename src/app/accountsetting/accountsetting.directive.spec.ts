import { AccountsettingDirective } from './accountsetting.directive';

describe('AccountsettingDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountsettingDirective();
    expect(directive).toBeTruthy();
  });
});
